<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >

    <!-- 表示shape的四个角的角度。只适用于矩形shape，这里的角度是指圆角的程度 -->
    <corners android:radius="@dimen/size_50dp" />

    <!-- 这个标签表示纯色填充，通过android:color即可指定shape中填充的颜色 -->
    <solid android:color="@color/color_15FD644B" />

    <padding
        android:left="@dimen/size_15dp"
        android:top="@dimen/size_3dp"
        android:right="@dimen/size_15dp"
        android:bottom="@dimen/size_3dp" />
</shape>